# docs

`architecture.html` is a self-contained interactive diagram (four views, dark/light).
GitHub will not render it inline — download it and open it in a browser, or read the
mermaid version at the top of the project README.

`architecture.json` is the source; the HTML is generated from it.
